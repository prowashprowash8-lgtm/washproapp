# Archive

Anciens scripts SQL (itérations de correctifs, versions superseded, scripts de diagnostic
ponctuels). Déplacés ici le 2026-07-06 suite à un audit externe qui notait 68 fichiers
`.sql` sans ordre clair à la racine du dépôt.

**Ne rejouer aucun de ces fichiers.** L'état réel de la base est dans Supabase (vérifiable
via `supabase db query --linked`), pas dans ces fichiers.
